# NeuroFocus Study Tool - Environment Variables Template
# Copy this file to .env.local and fill in your API keys
# 
# At minimum, configure at least one provider (SiliconFlow recommended as fallback)

# ============================================
# Multi-Provider Free Tier API Keys
# ============================================

# Groq API Key (Speed Layer - Fastest for short interactions)
# Get your API key from: https://console.groq.com/keys
# Free tier: 30 RPM, 6,000 TPM
GROQ_API_KEY=your_groq_api_key_here

# OpenRouter API Key (Last Resort Fallback - Very Limited Free Tier)
# Get your API key from: https://openrouter.ai/keys
# Free tier: ONLY 50 requests/day, 20 requests/min (unified across all models)
# With $10+ credits: 1,000 requests/day, 20 requests/min
# NOTE: 50/day is too limiting for regular use - only configured as last resort fallback
# The system will prioritize Groq/Gemini/SiliconFlow first, only using OpenRouter if all others fail
OPENROUTER_API_KEY=your_openrouter_api_key_here

# OpenRouter Model Selection (optional, only used if OpenRouter is selected)
# Choose which model to use from OpenRouter. All models share the same rate limit.
# Recommended free models for study tool:
#   - x-ai/grok-4.1-fast:free (Fast, general purpose - DEFAULT)
#   - meituan/chatglm-4-9b:free (Fast chat model)
#   - z-ai/glm-4.5-air:free (General purpose, good quality)
#   - openai/gpt-oss-20b:free (OpenAI's open source model)
#   - qwen/qwen-3-coder-480b-a35b:free (Very large, coding-focused)
# See all models: https://openrouter.ai/models
OPENROUTER_MODEL=x-ai/grok-4.1-fast:free

# SiliconFlow API Key (Volume Layer - Best for high throughput)
# Get your API key from: https://siliconflow.cn/
# Free tier: 1,000 RPM, 500,000 TPM
# OPTIONAL: App works without this - will use other available providers
SILICONFLOW_API_KEY=your_siliconflow_api_key_here

# Google Generative AI API Key (Vision/Context Layer - Handles images & long context)
# Get your API key from: https://aistudio.google.com/app/apikey
# Free tier: 15 RPM, 1,000,000 TPM
GOOGLE_GENERATIVE_AI_API_KEY=your_google_api_key_here

# HuggingFace API Key (Alternative Layer - Good free tier models)
# Get your API key from: https://huggingface.co/settings/tokens
# Free tier: Generous limits, good for complex reasoning tasks
# Uses OpenAI-compatible Inference API
HUGGINGFACE_API_KEY=your_huggingface_api_key_here

# GitHub Token (Intelligence Layer - Best reasoning, but strict rate limits)
# Get your token from: https://github.com/settings/tokens
# Free tier: 50 requests/day (very strict limit)
# Note: This uses GitHub's inference API, requires special access
GITHUB_TOKEN=your_github_token_here

# ============================================
# Queue Infrastructure (Upstash Redis)
# ============================================

# Upstash Redis URL (for queue management)
# Get your Redis URL from: https://console.upstash.com/
# Free tier: 10,000 commands/day, then pay-as-you-go
# Required for queue-based LLM request handling
UPSTASH_REDIS_REST_URL=your_upstash_redis_rest_url_here
UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_rest_token_here

# Vercel Cron Secret (for queue worker authentication)
# Generate a random secret string for securing the cron endpoint
# Example: openssl rand -base64 32
CRON_SECRET=your_random_secret_here

# Admin Panel Secret (for admin dashboard authentication)
# Generate a random secret string for securing the admin panel
# Example: openssl rand -base64 32
ADMIN_SECRET=your_admin_secret_here

# ============================================
# Usage Notes
# ============================================
# 
# The smart router automatically selects the best provider:
# - Groq: Default for short interactions (good free tier, fast) - RECOMMENDED
# - Groq: Fast short interactions - 30 RPM, 6K TPM
# - Gemini: Images, very long text (> 10,000 words), summaries - 15 RPM, 1M TPM
# - SiliconFlow: Long conversations (> 10 messages) or high volume - 1K RPM, 500K TPM (Chinese Qwen models)
# - HuggingFace: Complex reasoning tasks (good free tier models)
# - OpenRouter: LAST RESORT ONLY (50/day limit is too low for regular use)
# - GitHub GPT-4o: Complex reasoning tasks (very strict rate limits)
#
# If a provider's API key is missing, the system will automatically
# try other configured providers in order (Groq → Gemini → SiliconFlow → HuggingFace → OpenRouter → GitHub).
# OpenRouter is intentionally last due to its 50/day free tier limit.

